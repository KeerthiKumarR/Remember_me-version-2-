# Machine Learning Models

The model files in this directory (`*.onnx`) are automatically downloaded on backend startup if they are not already present.

To keep the repository size small and avoid tracking large binary files, they are excluded from version control in `.gitignore`.

Do not delete this `README.md` file, as it is tracked to ensure the directory structure remains in the repository.
